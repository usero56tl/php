node {
    def app
    stage('Clone') {
        checkout scm
    }
    stage('Build') {
        app = docker.build("usero/httpd")
    }
    stage('Test image') {
        withDockerContainer("usero/httpd"){ sh "echo 'hello world'" }
    }
}